## 2주차 AI 사용 기록
### 활용한 AI 및 용도
- LLM(chatGPT) 사용
- 파이썬 개념 확인 및 Git 명령어 확인
- 작성한 코드 및 보고서 검증


### 질문 및 답변 내용 정리
- Git에서 수정한 파일의 일부 변경사항만 골라서 커밋할 수 있는지 확인
    - `git add -p` 명령어를 사용하면 파일 전체가 아닌 변경된 부분을 선택하여 커밋할 수 있다.
    - Git이 제시하는 주요 선택지는 다음과 같다.
        - `y`: 해당 변경사항을 staging 영역에 추가한다.
        - `n`: 해당 변경사항을 추가하지 않는다.
        - `s`: 변경사항을 더 작은 단위로 나눈다.
        - `e`: 변경사항을 직접 수정하여 원하는 부분만 추가한다.
        - `q`: 작업을 종료한다.

###

- 커밋 메시지 수정 방법 확인
    - 가장 최근 커밋의 메시지 수정하고자 한다면 다음 명령어를 사용할 수 있다.
        -  `git commit --amend -m "새로운 커밋 메시지"`
    - 중간에 있는 커밋의 메시지 수정하고자 한다면 다음과 같이 할 수 있다.
        - `git rebase -i HEAD~3` ->
        - 수정하고자 하는 커밋의 pick을 reword로 변경한다. ->
        - Git이 커밋 메시지를 수정할 수 있게 해 준다.

###

- 파이썬 컨테이너 자료형의 원소 탐색 비용 확인
    - 리스트와 튜플은 인덱스를 이용한 접근은 O(1)이지만, 특정 값을 찾기 위해 탐색하는 경우 O(n)의 시간이 걸린다.
    - 집합은 해시 테이블을 기반으로 구현되어 있어 특정 값의 존재 여부를 확인할 때는 평균적으로 O(1)의 시간이 걸린다.
    - 사전 또한 해시 테이블을 기반으로 하므로, 키를 이용한 값의 접근은 평균적으로 O(1)의 시간이 걸린다.

###

- 파이썬의 class에 접근 제어를 위한 키워드가 있는지 확인
    - C++의 private와 같은 접근 제어 키워드는 파이썬에 존재하지 않는다. 대신 변수 이름 앞에 _ 또는 __를 붙이는 관례와 name mangling을 사용한다.
    - _변수는 외부에서 직접 사용하지 않는 것이 좋다는 의미의 관례일 뿐, 실제 접근을 제한하지는 않는다.
    - __변수는 Python이 내부적으로 이름을 변경하는 name mangling을 적용한다. 따라서 외부에서 직접 접근하기가 어려워지지만, 완전히 접근이 불가능해지는 것은 아니다.

###

- 파이썬의 `@property` 개념 확인
    - `@property`는 메소드를 일반적인 속성처럼 사용할 수 있도록 해 주는 기능이다.
    - 현재 data_structures.md에서 작성한 4번 시나리오(위도, 경도 매장 좌표 저장) 예시 코드에서는 튜플의 내부의 원소를 수정하는 것은 불가능하지만, location 속성 자체에 새로운 튜플을 다시 대입하는 것은 가능하다.
    - 한 번 발급된 좌표가 변경되지 않아야 한다는 요구사항을 더욱 잘 만족시키려면 다음과 같이 `@property`를 사용할 수 있다.

        ```python
        @property
        def location(self):
            return self._location
        ```
    - 별도의 setter를 정의하지 않으면 외부에서 location에 새로운 값을 대입하는 것이 불가능해진다. 따라서 location을 읽을 수는 있지만 변경할 수는 없는 읽기 전용 속성으로 만들 수 있다.